<div class="accordion-item">
  <button class="accordion-header" type="button">
    <span class="title">{{ title }}</span>
    <span class="icon">
      <img src="{{ 'plus-icon-white.svg' | asset_url }}" alt="Toggle">
    </span>
  </button>
  <div class="accordion-content">
    {{ content }}
  </div>
</div>
